// FIXME: Make me compile! Diff budget: 2 lines.
// this actually needs no changes because Rust now has built-in support for u128
// Do not modify this definition.
enum Duration {
    MicroSeconds(u128),
    MilliSeconds(u64),
    Seconds(u32),
    Minutes(u16)
}

fn main() { }
